{% extends '::base.html.twig' %}

{% block body -%}
    <h1>SousType list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Soustypename</th>
                <th>Path</th>
                <th>Db</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('soustype_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.SousTypeName }}</td>
                <td>{{ entity.path }}</td>
                <td>{{ entity.db }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('soustype_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('soustype_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('soustype_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
