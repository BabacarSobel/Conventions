{% extends '::base.html.twig' %}

{% block body -%}
    <h1>SousType</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Soustypename</th>
                <td>{{ entity.SousTypeName }}</td>
            </tr>
            <tr>
                <th>Path</th>
                <td>{{ entity.path }}</td>
            </tr>
            <tr>
                <th>Db</th>
                <td>{{ entity.db }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('soustype') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('soustype_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
