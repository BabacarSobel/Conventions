{% extends '::base.html.twig' %}

{% block body -%}
    <h1>FicheMessage creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('fichemessage') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
