{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Local list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>Disponible</th>
                <th>Capacite</th>
                <th>Batiment</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('local_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.nom }}</td>
                <td>{{ entity.disponible }}</td>
                <td>{{ entity.capacite }}</td>
                <td>{{ entity.batiment }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('local_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('local_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('local_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
