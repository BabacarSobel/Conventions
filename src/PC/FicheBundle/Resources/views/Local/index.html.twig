{% extends '::layout.html.twig' %}

{% block body -%}
    <div class="container">
        <h1>Liste des locaux</h1>
        <br>
        <a class="btn pc_skyblue_btn" href="{{ path('local_new') }}">Nouveau</a>  
        <hr>
        <table id="local_liste" class="table table-striped table-bordered table-hover">
            <thead>
                <tr>
                    <th>Id</th>
                    <th>Nom</th>
                    <th>Disponible</th>
                    <th>Capacite</th>
                    <th>Batiment</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
            {% for entity in entities %}
                <tr>
                    <td><a href="{{ path('local_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                    <td>{{ entity.nom }}</td>
                    <td>{{ entity.disponible }}</td>
                    <td>{{ entity.capacite }}</td>
                    <td>{{ entity.batiment }}</td>
                    <td>
                    <ul>
                        <li>
                            <a href="{{ path('local_show', { 'id': entity.id }) }}">Voir</a>
                        </li>
                        <li>
                            <a href="{{ path('local_edit', { 'id': entity.id }) }}">Modifier</a>
                        </li>
                    </ul>
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
    <script>
        $(document).ready( function () {
            $('#local_liste').DataTable();
        } );
    </script>
{% endblock %}
