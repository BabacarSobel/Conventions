{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Departement creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('departement') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
